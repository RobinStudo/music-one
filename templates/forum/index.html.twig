{% extends 'base.html.twig' %}

{% block title %} {{ parent() }}{% endblock %}

{% block body %}
    <h1>FAQ</h1>
    {% for topic in topics %}
         <h4>{{ topic.name }}</h4>
         <p>{{ topic.solution }}</p>
         </br>
    {% endfor %}
{% endblock %}
